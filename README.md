## Created this app using this command:
`npm create vite@latest event-management-system -- --template react`

## Project Description

This is a simple Event Management System that allows users to:
- Create events with basic details (title, description, date, time, location)
- View all created events
- Register for events
- Browse through different pages (Home, About, Services, Contact, Events)

The application is built using React with Vite and styled with Bootstrap for responsive design.

## Setup Instructions

### Prerequisites
- Node.js (version 14 or higher)
- npm or yarn

### Installation

1. Clone or download the project files
2. Navigate to the project directory in your terminal
3. Install dependencies:
   ```bash
   npm install
4. Start the development server:
    npm run dev
    